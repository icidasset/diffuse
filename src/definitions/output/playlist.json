{
  "lexicon": 1,
  "id": "sh.diffuse.output.playlist",
  "defs": {
    "main": {
      "type": "record",
      "record": {
        "type": "object",
        "required": ["id", "items", "name", "unordered"],
        "properties": {
          "id": { "type": "string" },
          "smart": {
            "type": "ref",
            "ref": "#smart"
          },
          "createdAt": { "type": "string", "format": "datetime" },
          "items": {
            "type": "array",
            "items": {
              "type": "ref",
              "ref": "#item"
            }
          },
          "name": {
            "type": "string"
          },
          "unordered": {
            "type": "boolean",
            "default": false,
            "description": "If this property is set to true, the playlist items should not be presented in order. It's considered an unordered collection of tracks."
          },
          "updatedAt": { "type": "string", "format": "datetime" }
        }
      }
    },
    "criterion": {
      "type": "object",
      "required": ["field", "value"],
      "properties": {
        "field": { "type": "string" },
        "transformations": {
          "type": "array",
          "items": { "type": "ref", "ref": "#transformation" }
        },
        "value": {
          "type": "unknown",
          "description": "Value that should be matched belonging to the given field"
        }
      }
    },
    "item": {
      "type": "object",
      "description": "Does not necessarily match one track. You can make it a smart playlist by having the criteria match multiple tracks.",
      "required": ["criteria"],
      "properties": {
        "criteria": {
          "type": "array",
          "items": { "type": "ref", "ref": "#criterion" }
        }
      }
    },
    "transformation": {
      "type": "string",
      "description": "Transformation to apply to the field value before comparison. This transformation is method to call on the value. For example, `toString`"
    }
  }
}
