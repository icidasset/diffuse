# Audio metadata artwork

This component implements an artwork component that retrieves artwork from an audio file using the resolved input url or stream.


## Requirements

- An `input-selector` attribute must be present so the component can reference the input component (used to resolve the track's url/stream).
