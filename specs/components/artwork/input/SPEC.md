# Input artwork

This component implements an artwork component that retrieves artwork from a given input component (see types).


## Requirements

- A `input-selector` attribute must be present to so the component can reference the input component.
